## 2026-03-28

### 2026-03-28 21:42
- 수정 파일
  - `Doc/portfolio/index.html`
  - `Doc/change_code/2026-03-28.md`
- 변경 내용
  - 연구실 컨택/포트폴리오 용도로 `Doc/portfolio` 폴더를 만들고, 현재 프로젝트 상태를 formal한 포트폴리오 형식으로 정리한 HTML 페이지를 추가했다.
  - 페이지 안에 프로젝트 개요, 현재 구현 구조, 실제 측정 근거, meaningful runtime log excerpt, 현재 병목, 향후 로드맵, 최종 목표를 한 화면에서 볼 수 있게 구성했다.
  - 시각 자료는 실제 프로젝트 자산인 `putty_log/plots/control_trace.png`, `putty_log/plots/latency_trace.png`를 연결했고, bench CSV excerpt와 runaway/fault 로그도 함께 넣어 의미 있는 evidence로 사용했다.
- 수정 이유
  - 연구실 면담에서 “무엇을 만들었는지”, “어디까지 검증했는지”, “현재 무엇이 남았는지”, “앞으로 어떻게 완성할 것인지”를 현업식 문서 형식으로 보여주기 위해서다.
- 영향 범위
  - 문서/포트폴리오 표현 계층만 변경되며, 펌웨어 동작에는 영향이 없다.

### 2026-03-28 22:06
- 수정 파일
  - `Core/Src/app_runtime.c`
  - `Doc/change_code/2026-03-28.md`
- 변경 내용
  - bench 확인을 위해 `APP_RUNTIME_ENCODER_DIAG_ENABLE`을 다시 `1`로 켰다.
  - `AppRuntime_Init()`에서 `HAL_TIM_Encoder_Start(&htim2, TIM_CHANNEL_ALL)`의 반환 상태와 초기 `CNT/CR1/SMCR/CCMR1/CCER`를 `[ENCSTART]` 한 줄로 남기도록 startup 로그를 추가했다.
- 수정 이유
  - 현재 TIM2 encoder path에서 startup 직후 상태와 런타임 상태를 분리해서 확인할 수 있도록 하기 위해서다.
  - encoder가 아예 시작 실패하는지, 아니면 시작은 됐지만 이후 입력 체인에서 문제가 생기는지 bench에서 빠르게 구분하려는 목적이다.
- 영향 범위
  - UART 로그가 100 ms 주기의 `[ENCDBG]`와 startup 1회 `[ENCSTART]`를 추가로 출력한다.
  - 제어 로직 자체는 바뀌지 않지만, bench 중 UART 출력량은 소폭 증가한다.

### 2026-03-28 22:28
- 수정 파일
  - `Doc/portfolio/index.html`
  - `Doc/change_code/2026-03-28.md`
- 변경 내용
  - 포트폴리오 HTML에 현업식 코드 audit 섹션을 추가해 startup auto-enable 경로, pulse/speed ceiling, watchdog timeout을 현재 코드 기준으로 분석했다.
  - `position_control` 모듈 소유권을 설명하는 6개 질문을 표로 정리하고, 현재 답할 수 있는 부분과 아직 남은 gap, 근거 로그를 같이 묶었다.
  - `INIT/READY/ACTIVE/SAFE/FAULT` 목표 상태전이 표와 SAFE/FAULT 구분 기준을 별도 섹션으로 추가하고, 현재 구현과 목표 설계를 구분해서 설명했다.
- 수정 이유
  - 연구실 면담에서 “현재 코드가 어디까지 구현되었는지”, “어떤 부분은 아직 설계 단계인지”, “안전과 상태전이를 어떻게 생각하고 있는지”를 보다 설계 문서답게 설명하기 위해서다.
- 영향 범위
  - 포트폴리오 문서 표현만 확장되며 펌웨어 동작에는 영향이 없다.

### 2026-03-28 22:41
- 수정 파일
  - `Doc/REQ/steering_project_req_ownership_guide.html`
  - `Doc/change_code/2026-03-28.md`
- 변경 내용
  - REQ HTML의 “엔코더 안정화 후 해야 할 수정” 섹션에, 외부 엔코더 피드백 테스트와 24V 릴레이 연결 테스트가 끝난 뒤 startup contract / SAFE-FAULT 상태기계 / watchdog 현실화 같은 수정을 진행하는 것이 맞다는 판단을 추가했다.
  - 현재 왜 이 수정들을 미루는지, 그리고 분리를 어떤 순서로 진행하면 bench를 덜 방해하면서 이해하기 쉬운지 단계별 설명과 표를 추가했다.
- 수정 이유
  - 현재 bench 단계에서 테스트 편의성과 원인 분리 가능성을 유지한 채, 나중에 어떤 순서로 구조 분리를 진행해야 하는지 REQ 문서에서 바로 이해할 수 있도록 하기 위해서다.
- 영향 범위
  - REQ/설계 문서 표현만 보강되며 펌웨어 동작에는 영향이 없다.
