# 2026-03-15

## 변경 요약

- 03-15-01: 상위 조향각 단위를 내부 제어 단위와 분리하기 위해 `steering_deg -> motor_deg` 변환 경로를 추가함
- 03-15-02: pulse용 line driver와 direction용 line driver를 구분하는 출력 경로로 코드 의미를 정리하고 shared enable 제어를 추가함
- 03-15-03: README와 코드 설명 문서를 실제 하드웨어 구조에 맞게 정리함
- 03-15-04: 날짜별 변경 이력을 누적 기록하는 `Doc/change_code` 운영 규칙을 추가함
- 03-15-05: 팀원별 문서의 REQ와 발전 항목을 자율주행 조향 fail-safe/debug 기준으로 보강함
- 03-15-06: 패킷 없이 UART 키보드 입력으로 조향 목표를 바꾸는 테스트 모드를 추가함
- 03-15-07: CubeMonitor용 디버그 변수와 선택형 DBG_LOOP 계측 훅을 프로젝트에 추가함
- 03-15-08: SysTick latency profiler 호출의 헤더 누락 경고를 수정함
- 03-15-09: UART 키보드 테스트 모드에 숫자 목표각 입력 기능을 추가함

## 03-15-01 03:25

- 파일:
  - `Core/Inc/constants.h`
  - `Core/Src/main.c`
  - `Core/Inc/ethernet_communication.h`
  - `Core/Src/ethernet_communication.c`
- 이유:
  - 상위기 입력값은 조향축 기준 각도인데, 내부 제어기는 모터축 기준 각도를 일부 전제로 두고 있어 단위 체계가 섞여 있었음
- 수정 내용:
  - `constants.h`에 `STEERING_GEAR_RATIO`, `SteeringDegToMotorDeg()`, `MotorDegToSteeringDeg()` 추가
  - `main.c`에서 UDP 수신 목표값과 초기 목표값을 내부 `motor_deg`로 변환해서 `PositionControl_SetTarget()`에 전달하도록 수정
  - `ethernet_communication.c`의 legacy `SET TARGET` 경로도 `motor_deg` 변환 후 제어기로 전달하도록 수정
  - 상태 응답과 DIAG 출력은 다시 `steering_deg` 기준으로 보이도록 변환해 출력
- 영향:
  - 외부 명령 단위와 내부 제어 단위의 역할이 코드상에서 더 명확해짐
  - 이후 `PositionControl_SetTarget()` API 이름 또는 내부 상태 구조를 더 명확히 바꾸기 쉬워짐

## 03-15-02 03:28

- 파일:
  - `Core/Inc/main.h`
  - `Core/Inc/pulse_control.h`
  - `Core/Src/pulse_control.c`
- 이유:
  - 실제 하드웨어가 pulse용 line driver와 direction용 line driver로 분리되어 있는데, 코드 의미와 주석은 단일 RS422 경로처럼 읽힐 여지가 있었음
- 수정 내용:
  - `main.h`에 `PR_TX_*`, `LINE_DRIVER_DE_*`, `LINE_DRIVER_REN_*` alias 추가
  - `pulse_control.h` 주석을 `PF+/PF-`, `PR+/PR-` 차동 출력 구조 기준으로 수정
  - `pulse_control.c`에 shared line driver enable helper 추가
  - `pulse_control.c`에 direction 적용 helper 추가
  - `PulseControl_Init()`, `PulseControl_SetFrequency()`, `PulseControl_SendSteps()`에서 분리된 pulse/direction 출력 경로 의미가 드러나도록 정리
- 영향:
  - 코드가 실제 하드웨어 구조를 더 잘 반영하게 됨
  - line driver enable과 direction 제어 경로를 한 곳에서 읽을 수 있게 됨
  - 현재는 shared enable 구조로 반영했고, 추후 pulse/direction 개별 enable 핀이 생기면 더 쉽게 확장 가능해짐

## 03-15-03 03:31

- 파일:
  - `README.md`
  - `Doc/code_modules.md`
- 이유:
  - 문서상 하드웨어 출력 구조가 최신 코드/배선 구조와 다르게 읽힐 수 있었음
- 수정 내용:
  - `PE9 -> pulse line driver -> PF+/PF-`
  - `PE10 -> direction line driver -> PR+/PR-`
  - `separate pulse/direction line drivers` 표현으로 상위 아키텍처 설명 정리
- 영향:
  - 코드와 문서 사이 하드웨어 해석 차이가 줄어듦
  - 팀원별 역할 문서와 메인 README의 설명이 더 일관되게 맞춰짐

## 03-15-04 04:05

- 파일:
  - `AGENTS.md`
  - `Doc/change_code/README.md`
  - `Doc/change_code/2026-03-15.md`
  - `Doc/change_code.md`
  - `Doc/README.md`
- 이유:
  - 앞으로의 코드 수정 이력이 날짜별로 누적되고, 같은 날 재수정된 내용도 추적 가능해야 했음
- 수정 내용:
  - 루트 `AGENTS.md`에 코드 수정 시 `Doc/change_code/YYYY-MM-DD.md`를 반드시 갱신하도록 규칙 추가
  - `Doc/change_code/README.md`에 날짜별 기록 형식과 같은 날 재수정 시 취소선 처리 규칙 문서화
  - 오늘 변경 내역을 `Doc/change_code/2026-03-15.md`에 누적 기록
  - 기존 단일 파일 경로를 보던 사용자를 위해 `Doc/change_code.md`에 폴더 기반 기록 방식으로 바뀌었음을 안내
  - `Doc/README.md`에 변경 이력 문서 위치를 최신 구조로 연결
- 영향:
  - 이후 코드 수정 시 날짜별 변경 근거를 일관되게 남길 수 있음
  - 같은 날 수정안이 바뀌어도 이전 판단과 최종 반영 내용을 함께 추적할 수 있음

## 03-15-05 04:32

- 파일:
  - `Doc/members/A.md`
  - `Doc/members/B.md`
  - `Doc/members/C.md`
  - `Doc/members/D.md`
  - `Doc/change_code/2026-03-15.md`
- 이유:
  - 기존 REQ와 발전 항목이 기능 구현 관점에는 유효했지만, 자율주행 조향모터의 완전한 구동, fail-safe, debug 완전성 기준에서는 fault latch, startup safety, sensor freshness, output inhibit 같은 현업형 요구가 더 필요했음
- 수정 내용:
  - A 문서에 stale target, latched fault, actual `dt`, saturation, deadline miss, recovery interlock 관련 REQ 추가
  - B 문서에 watchdog reset/brownout safe startup, homing timeout/retry, latched ESTOP clear, state transition log 관련 REQ 추가
  - C 문서에 sample freshness, implausible velocity, calibration persistence, commanded direction mismatch, bounded-time sensor fault handling 관련 REQ 추가
  - D 문서에 not-ready output inhibit, datasheet timing, driver enable separation, residual pulse/glitch 측정, 진단 구조체 요구를 추가
- 영향:
  - 팀원별 문서가 단순 모듈 설명에서 현업형 안전 요구와 검증 근거 중심 문서로 더 가까워짐
  - 이후 구현 우선순위를 fail-safe/debug 기준으로 더 선명하게 잡을 수 있음

## 03-15-06 04:58

- 파일:
  - `Core/Src/main.c`
  - `Doc/change_code/2026-03-15.md`
- 이유:
  - 현재 UDP 패킷 입력을 바로 준비하기 어려워, 벤치에서 최소한의 closed-loop 조향 동작을 빠르게 확인할 수 있는 수동 입력 경로가 필요했음
- 수정 내용:
  - `main.c`에 `KEYBOARD_TEST_MODE`를 추가해 UART3(ST-LINK VCP)로 키 입력을 폴링하는 테스트 경로를 추가
  - `A/D` 키로 steering target을 좌우 1도씩 변경하고, `S` center, `E` enable, `Q` disable, `X` estop, `H` help 명령을 추가
  - 키 입력은 조향축 기준 `steering_deg`로 받고 내부에서는 `SteeringDegToMotorDeg()`를 거쳐 기존 `PositionControl_SetTarget()` 경로를 그대로 사용하도록 구성
  - `KEYBOARD_TEST_MODE`가 켜진 동안에는 UDP 명령 대신 키보드 입력을 우선 사용하는 구조로 분기
- 영향:
  - 네트워크 패킷 없이도 encoder feedback 기반 closed-loop 조향 시험이 가능해짐
  - 상위 입력만 바뀌고 나머지 제어기, pulse 출력, relay 경로는 동일하게 재사용되어 벤치 검증에 유리해짐

## 03-15-07 05:24

- 파일:
  - `Core/Inc/debug_vars.h`
  - `Core/Src/debug_vars.c`
  - `Core/Inc/encoder_reader.h`
  - `Core/Src/encoder_reader.c`
  - `Core/Src/position_control.c`
  - `Doc/cubemonitor_setup_guide.md`
  - `Doc/change_code/2026-03-15.md`
- 이유:
  - STM32CubeMonitor로 target/position/error/output/fault를 실시간 시각화하려면 정적 글로벌 디버그 변수와 제어루프 계측 훅이 필요했음
- 수정 내용:
  - `debug_vars.h/.c`를 추가해 `dbg_enc_raw`, `dbg_pos_mdeg`, `dbg_target_mdeg`, `dbg_err_mdeg`, `dbg_pwm_cmd`, `dbg_fault_flags`를 정의
  - `encoder_reader`에 raw timer counter accessor를 추가
  - `position_control.c`에서 매 루프마다 디버그 변수를 갱신하도록 연결
  - `DBG_LOOP_Pin`이 `.ioc`에서 정의되면 `PositionControl_Update()` 시작/끝에서 자동 토글되도록 조건부 훅 추가
  - 프로젝트 전용 `CubeMonitor`/`.ioc` 설정 가이드를 문서로 추가
- 영향:
  - CubeIDE Watch와 CubeMonitor에서 바로 읽을 수 있는 최소 디버그 신호가 확보됨
  - 사용자가 `.ioc`에서 `DBG_LOOP` 핀만 추가하면 로직애널라이저용 루프 타이밍 측정도 바로 연동 가능해짐

## 03-15-08 20:41

- 파일:
  - `Core/Src/stm32f4xx_it.c`
  - `Doc/change_code/2026-03-15.md`
- 이유:
  - `SysTick_Handler()`에서 `LatencyProfiler_OnDeadlineTick()`를 호출하지만 헤더 include가 빠져 있어 implicit declaration warning이 발생했음
- 수정 내용:
  - `stm32f4xx_it.c`의 USER CODE include 구역에 `latency_profiler.h`를 추가
- 영향:
  - 빌드 경고 1건이 제거되어 `LatencyProfiler_OnDeadlineTick()`가 정식 프로토타입으로 컴파일됨

## 03-15-09 20:57

- 파일:
  - `Core/Src/main.c`
  - `Doc/cubemonitor_setup_guide.md`
  - `Doc/change_code/2026-03-15.md`
- 이유:
  - `A/D` 증분 입력만으로는 목표 조향각을 직접 지정하기 불편했고, 사용자가 절대 목표각을 바로 입력해 벤치 테스트하고 싶어 했음
- 수정 내용:
  - UART 키보드 테스트 모드에 숫자 버퍼를 추가
  - `5`, `-3.5`, `0`처럼 숫자를 입력하고 `Enter`를 누르면 해당 steering target으로 바로 이동하도록 추가
  - 기존 `A/D/S/E/Q/X/H` 명령은 유지하고, 명령 입력 시 미완료 숫자 버퍼는 자동 초기화되도록 정리
  - CubeMonitor 가이드 문서에도 숫자 목표각 입력 예시를 추가
- 영향:
  - 키보드 테스트만으로 증분 조작과 절대 목표각 조작을 모두 빠르게 시험할 수 있음

## 03-15-10 20:54

- 파일:
  - `Core/Src/position_control.c`
  - `Doc/change_code/2026-03-15.md`
- 이유:
  - `PositionControl_Enable()` 시작 줄에 잘못 입력된 문자열이 남아 있어 빌드가 실패했음
- 수정 내용:
  - `PositionControl_Enable()`의 깨진 토큰을 제거하고 `control_enabled = true;` 한 줄로 복구
- 영향:
  - `position_control.c` 컴파일 에러가 제거되어 전체 빌드가 다시 진행 가능해짐

## 03-15-11 21:06

- 파일:
  - `Core/Src/main.c`
  - `Doc/change_code/2026-03-15.md`
- 이유:
  - 실행 시 자동 latency 배치 로그가 나오지 않는 원인을 점검한 결과, profiler 초기화 호출이 빠져 있었고 키보드 테스트 모드에서는 `LAT_STAGE_COMMS` 샘플이 누적되지 않아 auto-report 조건이 영원히 만족되지 않았음
- 수정 내용:
  - `SystemClock_Config()` 직후 `LatencyProfiler_Init(SystemCoreClock);`를 다시 연결
  - `KEYBOARD_TEST_MODE` 경로의 `KeyboardTest_ProcessInput()`를 `LAT_STAGE_COMMS` 계측 구간으로 감싸 keyboard test 중에도 latency auto-report가 동작하도록 수정
- 영향:
  - UART 기준 latency batch 로그가 다시 출력될 수 있는 조건이 복구됨
  - Ethernet 없이 키보드 테스트만 하는 상태에서도 2000샘플 단위 자동 로그 수집이 가능해짐

## 03-15-12 21:19

- 파일:
  - `Core/Src/main.c`
  - `Doc/change_code/2026-03-15.md`
- 이유:
  - `+/-` 목표값을 주어도 물리 회전 방향이 같게 보이는 원인을 빠르게 분리하려면 target/current/error/output/DIR/encoder raw를 UART 터미널에서 즉시 확인할 수 있어야 했음
- 수정 내용:
  - 키보드 테스트 모드에 `KeyboardTest_PrintControlSnapshot()`를 추가
  - 목표값 적용 직후 현재 `T/C/E/O/DIR/ENC` 상태를 한 줄로 출력하도록 추가
  - `P` 키를 눌러 언제든 현재 제어 스냅샷을 출력할 수 있게 도움말과 입력 처리에 반영
- 영향:
  - 현장 테스트에서 encoder sign 문제인지, DIR 경로 문제인지, 목표값만 저장되고 제어 출력 부호가 안 바뀌는지 UART 로그만으로 더 빠르게 구분할 수 있음

## 03-15-13 21:34

- 파일:
  - `Doc/members/A.md`
  - `Doc/members/B.md`
  - `Doc/members/C.md`
  - `Doc/members/D.md`
  - `Doc/change_code/2026-03-15.md`
- 이유:
  - 이제 팀원별로 실제 구현을 배정해야 해서, 기존의 상위 REQ 초안만으로는 바로 개발 티켓으로 나누기 어려웠음
- 수정 내용:
  - A/B/C/D 문서 끝에 "지금부터 팀원에게 할당할 상세 개발 REQ" 섹션을 추가
  - 각 파트별로 현재 프로젝트에서 가장 시급한 문제를 기준으로 세부 구현 범위, 완료 기준, 검증 방법, 산출물을 함께 적은 실행형 REQ를 추가
  - sign 규약, startup state machine, encoder unwrap, direction polarity, stop/reverse 정책 등 현재 디버깅 이슈와 직접 연결되는 항목을 우선순위 높게 반영
- 영향:
  - 팀원별로 바로 개발 항목을 배정하고 진척을 관리할 수 있는 문서 형태가 갖춰짐
  - 이후 코드 변경과 측정 결과를 각 REQ ID에 직접 연결해 추적하기 쉬워짐
