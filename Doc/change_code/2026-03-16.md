## 03-16-01 00:15

- 파일:
  - `Core/Src/main.c`
  - `putty_log/plot_putty_log.py`
  - `Doc/keyboard_test_quick_ref.md`
  - `Doc/mclab_submission_pdf_outline.md`
  - `Doc/change_code/2026-03-16.md`
- 이유:
  - 제출용 그래프와 PDF를 만들기 위해 주기적 CSV 로그, PuTTY 로그 플로터, 키보드 테스트/hand-turn/fail-safe 가이드, MClab 제출용 PDF 목차 문서가 필요했음
- 수정 내용:
  - `main.c`에 100ms 주기의 periodic CSV 로그 출력을 추가하고 `L` 키로 on/off 토글 가능하게 반영
  - CSV 형식은 `ms,mode,target_deg,current_deg,error_deg,output,dir,enc` 기준으로 출력하도록 추가
  - `putty_log/plot_putty_log.py`를 추가해 PuTTY 로그에서 control trace와 latency trace PNG를 생성할 수 있게 함
  - `Doc/keyboard_test_quick_ref.md`를 추가해 키보드 명령, hand-turn encoder 확인 절차, fail-safe 캡처 절차를 정리
  - `Doc/mclab_submission_pdf_outline.md`를 추가해 DGIST MClab 제출용 PDF 목차와 그림 배치를 정리
- 영향:
  - PuTTY 로그만으로 그래프와 제출용 그림을 만들 수 있는 기반이 생김
  - 사용자가 UART 테스트와 fail-safe/encoder 확인 절차를 더 명확하게 수행할 수 있음

## 03-16-02 16:09

- 파일:
  - `Doxyfile`
  - `.gitignore`
  - `Doc/doxygen/html/*`
  - `Doc/change_code/2026-03-16.md`
- 이유:
  - 프로젝트의 사용자 코드 구조를 Doxygen + Graphviz로 로컬에서 시각화할 수 있는 기본 설정이 필요했음
- 수정 내용:
  - 프로젝트 루트에 `Doxyfile`을 추가해 `README.md`, `Core/Inc`, `Core/Src`만 문서화 대상으로 설정
  - `Drivers`, `LWIP`, `Middlewares` 대신 핵심 사용자 코드 중심으로 그래프가 보이도록 입력 범위를 제한
  - `CALL_GRAPH`, `CALLER_GRAPH`, `INCLUDE_GRAPH`, `DIRECTORY_GRAPH`를 활성화하고 `DOT_PATH`를 `C:/Program Files/Graphviz/bin`으로 지정
  - `syscalls.c`, `sysmem.c`, `system_stm32f4xx.c`, `Core/Startup`은 시각화 노이즈를 줄이기 위해 제외
  - `doxygen Doxyfile`를 1회 실행해 `Doc/doxygen/html/` 아래에 HTML 문서와 SVG 그래프가 실제로 생성되는 것까지 확인
  - `.gitignore`에 `Doc/doxygen/`를 추가해 Doxygen 생성 산출물이 Git 상태에 잡히지 않도록 설정
- 영향:
  - 로컬 PC에서 `doxygen Doxyfile`만 실행하면 `Core` 중심의 HTML 문서와 함수/포함 관계 그래프를 생성할 수 있음
  - Graphviz PATH가 즉시 반영되지 않았더라도 `DOT_PATH` 지정으로 문서 생성 성공 가능성이 높아짐
  - 문서 재생성 시 대량의 HTML/SVG 변경이 Git 작업트리를 어지럽히지 않게 됨
