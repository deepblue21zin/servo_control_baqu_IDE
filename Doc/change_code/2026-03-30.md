## 2026-03-30

### 2026-03-30 14:49
- 수정 파일
  - ~~`Doc/steering_portal/putty_live_viewer.html`~~
  - ~~`Doc/steering_portal/putty_live_viewer.css`~~
  - ~~`Doc/steering_portal/putty_live_viewer.js`~~
  - `Doc/steering_portal/index.html`
  - `Doc/steering_portal/app.js`
  - `README.md`
  - `Doc/README.md`
  - `Doc/change_code/2026-03-30.md`
- 대체 메모
  - 위 viewer 3개 파일은 같은 날 후속 수정에서 `Doc/putty/` 아래로 이동되어 현재 기준 경로가 아니다.
- 변경 내용
  - `putty.log`를 브라우저에서 읽어 `LATENCY`, `KB`, `CMD`, `CSV`, `ENCDBG`, `BOOT/SYSTEM` 타입으로 자동 분류하는 전용 로컬 뷰어를 추가했다.
  - 뷰어에 repo log live polling, 로컬 파일 권한 연결, snapshot 업로드, 자동 해석, 타입 필터, 상세 raw 보기, recording 세션 JSON 저장 기능을 넣었다.
  - 기존 steering portal의 로그 분석 절차와 deep link에 새 PuTTY live viewer 진입점을 추가했다.
  - portal 데이터 모델의 KPI와 Team F evidence 설명도 새 뷰어 기준에 맞게 보강했다.
  - `README.md`, `Doc/README.md`에 새 뷰어 위치와 사용 방식을 반영했다.
- 수정 이유
  - 사용자가 PuTTY로 디버깅하는 로그를 사이트에서 바로 보고, 기록 종류별로 나눠 확인하고, 간단한 해석과 recording 저장까지 한 흐름으로 쓰길 원했기 때문이다.
  - 기존 PNG plot 중심 분석만으로는 live bench 상황을 즉시 읽기 어려워, raw log 해석용 로컬 UI를 추가할 필요가 있었다.
- 영향 범위
  - 펌웨어 동작은 바뀌지 않고 문서/포털/로컬 로그 분석 도구만 추가된다.
  - 로컬 서버로 포털을 열면 `putty_log/putty.log`를 polling 하며 bench 로그를 실시간에 가깝게 볼 수 있다.

### 2026-03-30 18:44
- 수정 파일
  - `Doc/putty/index.html`
  - `Doc/putty/putty_live_viewer.css`
  - `Doc/putty/putty_live_viewer.js`
  - `putty_log/putty_live_bridge.py`
  - `putty_log/start_putty_live_viewer.ps1`
  - `Doc/steering_portal/index.html`
  - `Doc/steering_portal/app.js`
  - `README.md`
  - `Doc/README.md`
  - `Doc/change_code/2026-03-30.md`
- 변경 내용
  - PuTTY live viewer를 `Doc/steering_portal`에서 `Doc/putty` 폴더로 이동했다.
  - viewer에 Python bridge 기반 live stream 연결을 추가해서, `putty.log`에 새 줄이 생기면 polling보다 더 빠르게 반영되도록 바꿨다.
  - `putty_log/putty_live_bridge.py`를 추가해 snapshot API와 SSE-style live line stream을 제공하도록 했다.
  - `putty_log/start_putty_live_viewer.ps1`를 추가해 bridge, 브라우저, PuTTY logging을 한 번에 띄울 수 있게 했다.
  - README, 문서 인덱스, steering portal deep link를 새 `Doc/putty/index.html` 경로와 launcher 기준으로 동기화했다.
- 수정 이유
  - 사용자가 viewer를 `putty` 전용 폴더로 분리하길 원했고, 동시에 “실시간으로 못 보지 않느냐”는 우려를 줄이기 위해 bridge 기반 live 구조가 필요했기 때문이다.
  - 단순 파일 polling보다 더 즉각적인 bench 관찰 흐름과 실행 편의성을 같이 제공하려는 목적이다.
- 영향 범위
  - 펌웨어 동작에는 영향이 없고, 로컬 PC 쪽 PuTTY 로그 수집/표시 도구만 확장된다.
  - `putty_log/start_putty_live_viewer.ps1`를 쓰면 bridge 서버와 브라우저가 같이 열리고, COM 포트가 맞으면 PuTTY logging도 동시에 시작된다.

### 2026-03-30 19:09
- 수정 파일
  - `Doc/members/member_brief.css`
  - `Doc/members/adc_encoder.html`
  - `Doc/members/pulse_control.html`
  - `Doc/members/homing_relay.html`
  - `Doc/steering_portal/index.html`
  - `Doc/steering_portal/app.js`
  - `Doc/steering_portal/styles.css`
  - `README.md`
  - `Doc/README.md`
  - `Doc/change_code/2026-03-30.md`
- 변경 내용
  - `Doc/members` 아래에 ADC + encoder, pulse control, homing + relay 담당자용 상세 HTML 할당 문서 3종을 새로 만들었다.
  - 각 문서에 현재 코드에서 이미 구현된 내용, 아직 부족한 점, 왜 부족한지, 다음 개발 REQ, 완료 기준, evidence 항목을 현재 코드 기준으로 정리했다.
  - steering portal Team B/C/D 카드에 상세 할당서 링크를 추가하고, deep link 영역에도 새 member brief 3종을 연결했다.
  - `README.md`와 `Doc/README.md` 문서 인덱스에 새 역할 문서를 추가해 바로 접근할 수 있게 했다.
- 수정 이유
  - 사용자가 실제 팀원 3명에게 다음 작업을 곧바로 할당할 수 있도록, REQ와 현재 코드 상태를 연결한 실행형 문서가 필요했기 때문이다.
  - 기존 `A.md`, `B.md`, `C.md`, `D.md`는 내용이 풍부하지만 markdown 기반이고 일부 사실이 최신 코드와 어긋나 있어, 현재 baseline 기준 HTML 브리프가 새로 필요했다.
- 영향 범위
  - 펌웨어 실행 로직은 바뀌지 않고 문서, 포털 링크, 팀 할당 자료만 추가/갱신된다.
  - 앞으로는 Sensor / Pulse / Homing-Relay 담당자에게 각각 HTML 한 장씩 바로 전달해 현재 구현과 남은 개발 범위를 설명할 수 있다.

### 2026-03-30 21:17
- 수정 파일
  - `Doc/matlab_simulink_application_plan.md`
  - `README.md`
  - `Doc/README.md`
  - `Doc/steering_portal/index.html`
  - `Doc/steering_portal/app.js`
  - `Doc/change_code/2026-03-30.md`
- 변경 내용
  - 현재 `servo_control_baqu` 구조에 MATLAB / Simulink를 어떻게 적용해야 하는지 단계별 계획 문서를 새로 만들었다.
  - 문서 안에 current firmware monitoring, shadow model, export-function codegen, sandbox target monitor, PIL 순서를 현재 코드 구조 기준으로 정리했다.
  - README, Doc 인덱스, steering portal deep link에 새 MATLAB / Simulink 계획 문서 진입점을 추가했다.
- 수정 이유
  - 사용자가 지금 프로젝트 기준으로 MATLAB / Simulink 적용 방법을 Doc 안에 정확한 실행 계획 형태로 정리해두길 원했기 때문이다.
  - 단순 툴 소개가 아니라 현재 runtime 구조, UART 사용, `.ioc` 경계, 파라미터 truth 분리까지 반영한 프로젝트 맞춤형 문서가 필요했다.
- 영향 범위
  - 펌웨어 C 소스 동작은 바뀌지 않고 문서와 포털 안내만 보강된다.
  - 이후 MATLAB / Simulink 적용 작업은 새 문서를 기준으로 host 분석, shadow model, export-function, target monitor를 순서대로 진행할 수 있다.

### 2026-03-30 21:25
- 수정 파일
  - `Doc/members/position_control.html`
  - `Doc/members/ethernet_integration.html`
  - `Doc/members/verification_tooling.html`
  - `README.md`
  - `Doc/README.md`
  - `Doc/steering_portal/index.html`
  - `Doc/steering_portal/app.js`
  - `Doc/change_code/2026-03-30.md`
- 변경 내용
  - `Doc/members` 아래에 남은 워크스트림인 position control, ethernet/system integration, verification/tooling 담당 상세 HTML 문서 3종을 추가했다.
  - 각 문서에 현재 코드 기준 구현 상태, 부족한 점, 왜 부족한지, 다음 개발 REQ, evidence 기준을 현재 baseline에 맞춰 정리했다.
  - steering portal Team A/E/F 카드에도 상세 할당서 링크를 붙이고, deep link 영역에 새 member brief 3종을 추가했다.
  - README와 Doc 인덱스에서 member brief를 6종 전체 기준으로 찾을 수 있게 갱신했다.
- 수정 이유
  - 사용자가 기존 3개 역할뿐 아니라 남은 REQ도 position_control, ethernet 등으로 자세히 나눠 문서화하길 원했기 때문이다.
  - 팀원 전체에게 바로 배정 가능한 current-code-based 실행 문서 세트를 완성하려는 목적이다.
- 영향 범위
  - 펌웨어 동작에는 영향이 없고, 문서/포털/팀 할당 자료만 확장된다.
  - 이제 Team A~F 모든 워크스트림을 `Doc/members` HTML 문서로 직접 배정하고 추적할 수 있다.

### 2026-03-30 21:52
- 수정 파일
  - `Core/Inc/project_params.h`
  - `Core/Inc/position_control.h`
  - `Core/Inc/pulse_control.h`
  - `Core/Inc/ethernet_communication.h`
  - `Core/Inc/latency_profiler.h`
  - `Core/Src/app_runtime.c`
  - `Core/Src/encoder_reader.c`
  - `Core/Src/ethernet_communication.c`
  - `Core/Src/position_control.c`
  - `Core/Src/position_control_safety.c`
  - `Core/Src/pulse_control.c`
  - `Doc/README.md`
  - `Doc/change_code/2026-03-30.md`
- 변경 내용
  - `project_params.h`를 실제 중앙 설정 파일로 승격하고, 각 항목에 어느 코드 경로에서 쓰는 설정인지 주석을 추가했다.
  - `app_runtime`, `position_control`, `pulse_control`, `ethernet_communication`, `latency_profiler` 쪽에 흩어져 있던 중복 매크로 정의를 제거하고 `project_params.h`를 참조하도록 연결했다.
  - `position_control.c`의 PID 기본값 초기화도 하드코딩 대신 `DEFAULT_*` 매크로를 사용하도록 바꿨다.
  - `encoder_reader.c`와 `app_runtime.c`에 기존에 만들어만 두고 실제로는 쓰이지 않던 boot/init 스위치를 연결하되, 기본값은 현재 펌웨어 동작과 같게 맞췄다.
  - 검증 중 발견된 `Doc/README.md` trailing whitespace도 같이 정리했다.
- 수정 이유
  - 사용자가 여러 코드에 흩어진 설정값을 한 곳에서 관리하고, `project_params.h`만 봐도 어떤 모듈 설정인지 바로 알 수 있길 원했기 때문이다.
  - 기존에는 중앙 설정 파일이 있어도 실제 소스가 그 값을 쓰지 않아 값 불일치와 유지보수 혼선이 생길 수 있었다.
- 영향 범위
  - 펌웨어 동작 baseline은 유지하면서, 운영/튜닝 파라미터의 관리 지점이 `project_params.h` 중심으로 정리된다.
  - 이후 주파수 clamp, PID 기본값, UDP timeout, latency 설정, app runtime bench switch를 한 파일에서 일관되게 조정할 수 있다.

### 2026-03-30 22:35
- 수정 파일
  - `Core/Inc/project_params.h`
  - `Core/Src/encoder_reader.c`
  - `Doc/change_code/2026-03-30.md`
- 변경 내용
  - `project_params.h` 상단 설명에 “내부 구현 상수는 각 모듈 로컬에 둔다”는 기준을 명시했다.
  - 사람 손으로 조정할 파라미터가 아닌 `ENCODER_READER_FORCE_CENTER_ON_INIT` 항목을 `project_params.h`에서 제거했다.
  - `encoder_reader.c`는 원래 baseline 동작대로 초기화 시 카운터를 center로 맞추는 구현을 로컬 상수 기반으로 유지하도록 정리했다.
- 수정 이유
  - 사용자가 `project_params.h` 안에 실제로 사람이 건드려야 할 값만 남아 있는지 확인해달라고 요청했기 때문이다.
  - encoder counter center 초기화는 튜닝 파라미터보다 구현 디테일에 가까워 중앙 설정 파일에 두면 오히려 오해를 부를 수 있었다.
- 영향 범위
  - `project_params.h`는 운영/튜닝/디버그/연동 설정 중심으로 더 선명해진다.
  - encoder 초기화 동작은 기존 baseline과 동일하게 유지되며, 이번 정리는 설정 분류만 더 명확하게 만든다.
